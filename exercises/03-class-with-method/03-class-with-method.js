/**
 * TODO: add an `incrementSomeNumber` method in `ClassWithMethod` that increments `someNumber`
 */

class ClassWithMethod {
  constructor() {
    this.someNumber = 5;
  }
}

module.exports = ClassWithMethod;
