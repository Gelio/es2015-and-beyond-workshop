const age = 42;

const regularAge = 'The age is ' + age;

const templateAge = `The age is ${age}`;

const multilineString = 'Line one\nLine two\nLine three';

const multilineTemplateString = `Line one
Line two
Line three`;

module.exports = {
  regularAge,
  templateAge,
  multilineString,
  multilineTemplateString
};
