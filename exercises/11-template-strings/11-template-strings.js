/**
 * NOTE:
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals
 */

const age = 42;

const regularAge = 'The age is ' + age;

/**
 * TODO: complete the `templateAge` variable to be the same as `regularAge` but using a template
 * string
 */
const templateAge = null;

const multilineString = 'Line one\nLine two\nLine three';

/**
 * TODO: complete the `multilineTemplateString` variable to be the same as `multilineString` but
 * using a template string.
 *
 * NOTE: Do not use the `\n` character explicitly
 */
const multilineTemplateString = null;

module.exports = {
  regularAge,
  templateAge,
  multilineString,
  multilineTemplateString
};
